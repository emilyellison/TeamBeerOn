<!DOCTYPE html>
<html>
<head>
  <title>BeerOn</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header>
	<h1>BeerOn</h1>
		<p class='menu'>
			<a href='#'>Contact Us</a>
			<% if @current_member %>
				<%= link_to "Profile", member_url(@current_member.id) %>
				<%= link_to "Sign Out", sign_out_url %>	</p>
			<% else %>
				<%= link_to "Sign Up", new_member_url %>
				<%= link_to "Sign In", new_session_url %>
				</p>
			<% end %>
		<hr/>

		<form class='search'/>
			<input type='text' id='query' name='query'></input>
			<input type='submit' value='Search' class='button'></input>
		</form>

		<hr/>
	</header>
	<div class='content'>
		<% if flash[:notice] %>
			<%= flash[:notice] %>
		<% end %>
		<%= yield %>
	</div>
</body>
</html>
